@model MajorsPool.Models.TopPickList

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TopPickList</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.EntrantId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EntrantId)
            @Html.ValidationMessageFor(model => model.EntrantId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
