@model MajorsPool.Models.TopPickList

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>TopPickList</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.EntrantId)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.EntrantId)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.TopPickListId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
